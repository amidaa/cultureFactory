<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.first.group.dao.UserAuthorityMapper">
  <resultMap id="BaseResultMap" type="com.first.group.entity.UserAuthority">
    <result column="username" jdbcType="VARCHAR" property="username" />
    <result column="authorityid" jdbcType="VARCHAR" property="authorityid" />
  </resultMap>
  <insert id="insert" parameterType="com.first.group.entity.UserAuthority">
    insert into user_authority (username, authorityid)
    values (#{username,jdbcType=VARCHAR}, #{authorityid,jdbcType=VARCHAR})
  </insert>
  <insert id="insertSelective" parameterType="com.first.group.entity.UserAuthority">
    insert into user_authority
    <trim prefix="(" suffix=")" suffixOverrides=",">
      <if test="username != null">
        username,
      </if>
      <if test="authorityid != null">
        authorityid,
      </if>
    </trim>
    <trim prefix="values (" suffix=")" suffixOverrides=",">
      <if test="username != null">
        #{username,jdbcType=VARCHAR},
      </if>
      <if test="authorityid != null">
        #{authorityid,jdbcType=VARCHAR},
      </if>
    </trim>
  </insert>
    <select id="findOne" resultType="com.first.group.entity.UserAuthority">

      select username,authorityid from user_authority
      <where>
        <if test="_parameter!=null">
          username=#{username}
        </if>
      </where>

    </select>
</mapper>